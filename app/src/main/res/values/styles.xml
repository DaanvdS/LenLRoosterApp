<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="actionBarStyle">@style/ActionBarLL</item>
    </style>
    <style name="ActionBarLL" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/boven</item>
        <item name="background">@color/boven</item>
    </style>
</resources>
